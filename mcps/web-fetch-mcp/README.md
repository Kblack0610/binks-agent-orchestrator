# Web Fetch MCP — Merged into web-search-mcp

The fetch tools (`fetch`, `fetch_json`, `parse_html`, `fetch_markdown`) have been merged directly into `web-search-mcp` rather than maintained as a separate crate.

See [`../web-search-mcp/`](../web-search-mcp/) for the implementation.

This directory is kept as a historical reference only.
